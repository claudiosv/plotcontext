# Examples

Runnable notebooks showing off `plotcontext`'s context managers and
helpers. Each page is executed at doc-build time, so the plots you see are
generated by the current version of the library.
